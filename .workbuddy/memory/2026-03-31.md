# 2026-03-31 工作记录

## 地图添加购物中心图层开关 (15:10)
- 「显示门店」面板新增「购物中心」开关（默认隐藏）
- 购物中心图标样式：星形（star），橙色 #e6a23c
- 支持拖拽更新坐标（带确认对话框）
- 支持筛选联动（购物中心管理页筛选时地图自动过滤）
- shoppingCenterStore 添加 visibleIds 支持
- 已部署到服务器

## 修复筛选条件跨用户同步问题 (15:05)
- **问题**：管理员在品牌门店/购物中心页面的筛选条件，通过 Pinia store.filters 持久化后被普通用户 onMounted 恢复，导致普通用户看到管理员遗留的筛选状态
- **修复**：BrandStoreView.vue 和 ShoppingCenterView.vue 的 onMounted 改为重置筛选条件而非从 store 恢复；同时移除 watch 对 store.filters 的监听
- **保留**：操作筛选时仍写入 store.filters（用于地图 visibleIds 联动），只是进入页面时不再读取
- 已部署到服务器

## 新增购物中心页面 (14:16)
- **数据库**：新增 `shopping_centers` 表（store_code, name, store_category, city, district, address, rank_info, comments, stars, latitude, longitude, status, icon_color）
- **后端**：`/api/shopping-centers` 路由（GET/POST/PUT/DELETE + batch-delete/clear-all/import/export），仅管理员可写
- **前端**：ShoppingCenterView.vue（品牌门店为模板）、shoppingCenterStore.js
- **导航**：品牌门店后新增「购物中心」入口（Shop 图标）
- **已部署**：前后端均已部署，PM2 重启完成

## 隐藏地图工具箱「图标样式」按钮 (13:55)
- 注释掉 MapView.vue 中图标样式 el-tooltip+el-popover 代码块
- 已部署到服务器

## 修复面积计算错误 (09:30)
- **问题**：`calculateArea` 使用简化平面 Shoelace 公式，误差大；`formatArea` 单位换算错误（10000㎡误作1平方公里，应为1,000,000㎡）
- **修复**：
  - 改用球面多边形面积公式（基于地球半径 6371000m，积分法），精度大幅提升
  - 格式化单位：< 10000㎡ 显示平方米，< 1,000,000㎡ 显示公顷，≥ 1,000,000㎡ 显示平方公里
- **修改文件**：`frontend/src/utils/map.js`
- **已部署**：vite build + SFTP 同步到服务器
