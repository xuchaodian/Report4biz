# 2026-04-13 工作日志

## 联通人口面板优化

### 1. 数据年月逻辑修复
- 修改了 `loadAvailableMonths()` 函数，4月时提供3月和2月数据
- 影响文件：`SmartstepsPanel.vue`、`StoreSmartstepsDialog.vue`

### 2. 按钮文字修改
- "购买查询" → "购买"
- 影响文件：`SmartstepsPanel.vue`、`StoreSmartstepsDialog.vue`

### 3. 配额检查逻辑统一
- **SmartstepsPanel.vue**（地图联通人口面板）：
  - 添加 `quotaInfo.value?.available > 0` 检查
  - 购买按钮在配额为0时显示为灰色不可点击
- **StoreSmartstepsDialog.vue**（门店对话框）：
  - 已有配额检查逻辑

### 4. disabled 样式优化
- 按钮 disabled 时背景色改为灰色（#dcdfe6）
- 影响文件：`SmartstepsPanel.vue`

### 5. 部署问题修复
- 服务器 assets 目录未正确上传
- 后端 axios 模块在某些重启后未加载成功
- 解决方案：重新 npm install axios，重启 PM2

## 版本
- GeoManager v1.3.10
