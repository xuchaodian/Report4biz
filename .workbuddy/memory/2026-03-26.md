# 2026-03-26 工作日志

## 问题修复：门店不在地图上显示

### 原因
服务器上的 `markers.js` 是旧版本（26个?占位符），而服务器数据库表已是新版（27列，不含id），导致 INSERT 报错 "26 values for 27 columns"。

### 修复
重新上传本地修复版的以下两个文件到服务器：
- `/backend/src/routes/markers.js`
- `/backend/src/models/database.js`

重启 PM2 后验证 API 添加门店返回 `{"message":"添加成功"}`，问题解决。

## 问题修复（第三轮）：门店仍不显示

### 问题排查
- 服务器API正常（9条门店，坐标正确）
- 问题定位在前端 JS

### 修复内容
1. `createCustomIcon`：emoji在divIcon中可能渲染失败 → 改为纯内联CSS水滴形图标
2. `businessLayer.setStyle()`：layerGroup无此方法会报错 → 删除该调用
3. `updateLayerDisplay()`：`hasLayer(null)` 会报错 → 增加 null 判断
4. `loadMarkers()`：整体加 try-catch，防止单个marker或热力图报错中断整个加载流程
5. 过滤无坐标的门店数据

### 部署
重新构建前端 dist 并 rsync 上传服务器，reload nginx。


## 新增：用户管理 - 重置密码功能

用户管理界面操作列新增"重置密码"按钮（🔄图标），点击后确认对话框，重置该用户密码为 `123456`。

后端：`POST /api/users/:id/reset-password`
前端：`UsersView.vue` - handleResetPassword 函数

## 修复：门店管理跳转地图定位

问题：DataView.vue 点击地图图标跳转到 `/` 路由时，MapView.vue 没有处理 `route.query` 参数，导致飞不到对应位置。

修复：MapView.vue 的 `onMounted` 中，读取 `route.query.lat/lng`，飞到此坐标（zoom=15）。

修改文件：`frontend/src/views/MapView.vue`（引入 useRoute + onMounted 中处理跳转参数）

## 修复：门店图标颜色改为按门店类型（store_type）区分

图标颜色从 `getStoreCategoryColor(store_category)` 改为 `getStoreTypeColor(store_type)`，颜色含义：
- 已开业 → 绿色
- 重点候选 → 橙色
- 一般候选 → 灰色

修改文件：`frontend/src/views/MapView.vue` 两处（普通模式和聚合模式）

## 数据修复：store_type 与 store_category 字段值装反

### 问题
- 前端定义：store_type = 已开业/重点候选/一般候选；store_category = 社区店/临街店/商场店...
- 数据库示例数据写反：store_type存了位置类型，store_category存了直营/加盟

### 修复
1. 用 Python 脚本批量调用 PUT API 互换现有8条数据的两个字段值
2. 修正 database.js 示例数据，防止重新初始化时再次写入错误数据
3. 上传 database.js 到服务器

## UI 改动（图层面板）
- 去掉了清新地图、远山黛、月光银、雅士灰等灰色底图（高德瓦片 style 参数无法实现真正灰色底图，需要高德 Key + 自定义样式才能使用）
- 图层面板当前只保留：矢量地图 + 影像地图
- 待用户申请高德 Key 后再添加灰色底图样式（amap://styles/grey 等）
