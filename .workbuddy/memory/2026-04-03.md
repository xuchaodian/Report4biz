# 2026-04-03 工作记录

## Shapefile 文件重命名功能 (17:44)
- **后端**：`backend/src/routes/shapefiles.js` 新增 `PUT /api/shapefiles/:id/rename` 接口
- **前端**：`ShapefileView.vue` 文件名列支持内联编辑
  - 鼠标悬停显示编辑图标，点击或双击进入编辑模式
  - Enter 确认、Esc 取消，编辑框失焦保留（等待确认/取消按钮操作）
- **部署**：前端 vite build + SFTP 部署 + 后端文件同步 + PM2 重启
