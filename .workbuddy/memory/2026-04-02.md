# 2026-04-02 日记

## 腾讯地图Y轴转换修复

### 问题
Leaflet加载腾讯地图瓦片时出现403拒绝、瓦片全黑/空白等问题。

### 原因
腾讯地图使用TMS坐标系，Y轴计算公式为：`y = 2^z - 1 - leaflet_y`
之前的代码只设置了 `yx: true`，但这不够。

### 解决方案
创建自定义 `L.TencentTileLayer` 类处理Y轴转换：

```javascript
L.TencentTileLayer = L.TileLayer.extend({
  getTileUrl: function(tilePoint) {
    const z = tilePoint.z
    const x = tilePoint.x
    const y = Math.pow(2, z) - 1 - tilePoint.y  // 关键转换
    const s = this._getSubdomain(tilePoint)
    return this._url
      .replace('{s}', s)
      .replace('{z}', z)
      .replace('{x}', x)
      .replace('{y}', y)
  }
})
```

### 用户偏好
- 部署完成后直接让用户自己测试，不调用 preview_url

## 高德地图搜索框修复 (2026-04-02 下午)
### 功能
- 使用后端高德搜索建议API（`/api/geocode/suggest`）进行模糊检索
- 实时搜索：输入时自动搜索（300ms防抖）
- 回车键直接跳转到第一个搜索结果

### 样式调整（高德地图风格）
- 搜索框宽度增加到 360px
- 圆角 8px，蓝色边框阴影
- 搜索按钮改为输入框内前缀图标
- 结果列表：圆角8px，显示名称和地址两行，圆形图标
